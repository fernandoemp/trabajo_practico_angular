import { FormatearDniPipe } from './formatear-dni.pipe';

describe('FormatearDniPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearDniPipe();
    expect(pipe).toBeTruthy();
  });
});
