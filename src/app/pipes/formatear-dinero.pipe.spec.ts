import { FormatearDineroPipe } from './formatear-dinero.pipe';

describe('FormatearDineroPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearDineroPipe();
    expect(pipe).toBeTruthy();
  });
});
